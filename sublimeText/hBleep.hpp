#include <string>

//decl fun:

//function to change the characters in the text into asterisk at position of the outer loop "i" 
void asterisks(std::string word, std::string &text, int i);

//function to check each characters and comparing them to the word assigned.
void bleep(std::string word, std::string &text);