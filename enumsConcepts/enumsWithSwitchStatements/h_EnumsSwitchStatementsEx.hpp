// Example of enum class for using it inside a switch statement
// Here adding declarations:
#include <iostream>

enum class Shape {
    CIRCLE,
    SQUARE,
    TRIANGLE
};

void printShapeMessage(Shape shape);