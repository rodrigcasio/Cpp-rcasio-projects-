// enum class example of HTTPS code returns 

enum StatusCode {
    SUCCESS = 1,
    ERROR = 0,
    NOT_FOUND = 404,
    UNAUTHORIZED = 401,
    FORBIDDEN = 403,
    INTERNAL_ERRROR = 500,
    WARNING = 199
};
